<?xml version="1.0" encoding="utf-8"?>
<!-- 战靴里程 widget. Kept to the RemoteViews subset (LinearLayout / TextView /
     ImageView / ProgressBar) — a widget layout is inflated by the launcher, not
     by us, so ConstraintLayout and custom views are not available.

     One layout for every size: the launcher resizes the frame and the weights
     absorb it. A second, small-only layout would double what has to stay in
     sync for a size most people never pick. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/sf_widget_background"
    android:padding="14dp">

    <!-- Shown when there is nothing to report: signed out, empty closet, or the
         widget switched off in the app's settings. -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:visibility="gone"
        android:textSize="13sp"
        android:textColor="?android:attr/textColorSecondary"
        android:text="" />

    <LinearLayout
        android:id="@+id/widget_body"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/widget_shoe_image"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:scaleType="fitCenter"
                android:adjustViewBounds="true"
                android:contentDescription="@string/sf_widget_closet_label"
                android:layout_marginEnd="10dp" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/widget_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.06"
                    android:textColor="?android:attr/textColorSecondary"
                    android:text="" />

                <TextView
                    android:id="@+id/widget_headline"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:maxLines="1"
                    android:textColor="?android:attr/textColorPrimary"
                    android:text="" />
            </LinearLayout>
        </LinearLayout>

        <ProgressBar
            android:id="@+id/widget_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginTop="10dp"
            android:max="100"
            android:progress="0" />

        <TextView
            android:id="@+id/widget_shoe"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="?android:attr/textColorPrimary"
            android:text="" />

        <TextView
            android:id="@+id/widget_stats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="?android:attr/textColorSecondary"
            android:text="" />
    </LinearLayout>
</LinearLayout>
